# Инструкции для AI Агента

> **Общее правило:** Все инструкции далее трактовать как "Если не указано явно иное, то ..."

---

## 🌐 Языковые настройки

- **Язык кода:** английский
- **Язык общения:** русский
- **Комментарии в коде:** английский, минимум
- **Принцип:** код должен быть самокомментированным

---

## Генерация кода (если не указано иное, то...)
* всегда проверять актуальность кода в открытых источниках используемых библиотек и фреймворков
* использовать только актуальные версии библиотек и фреймворков
* избегать использования устаревших методов и функций
* при генерации кода стараться использовать современные стандарты и лучшие практики программирования
* методы и функции должны быть максимально типизированны (использовать type hints и Pydantic в Python)
* по возможности добавлять к методам тип возвращаемого значения
* во время написания кода придерживаться принципов чистого кода (Clean Code) и SOLID
* стараться писать код, который легко тестировать и поддерживать
* избегать избыточной сложности и дублирования кода (DRY)
* использовать асинхронное программирование там, где это уместно
* если не указано иное
  * использовать асинхронные методы и функции (async/await) для операций ввода-вывода и сетевых запросов
  * не генерить файлы отчетов .md
  * не генерить тестовые скрипты и данные
* не нужно проверять досконально решение после его реализации, если не указано иное. не надо проверять, применились ли изменения.
* не нужно проверять, применились ли изменения
* если есть изменения в коде, после внесения изменений обновлять документацию метода/класса/модуля (docstrings)
* usage_example_*.py, test_*.py без явного указания не генерить

## 🛠️ Среда разработки

### Основные параметры
- **IDE:** JetBrains IDEA
- **Shell:** PowerShell (Windows) - команды для run_in_terminal должны быть PowerShell-совместимыми. ОСЬ - Windows.

### PowerShell команды (важно!)
При использовании `run_in_terminal` tool:
- ✅ Разделитель команд: `;` (НЕ `&&`)
- ✅ Чтение файлов: `type` (НЕ `cat`)
- ✅ Использовать только PowerShell-совместимые команды
- ✅ Команды 'head' и 'tail' НЕ использовать, вместо них использовать:
  - `Get-Content -TotalCount n` для чтения первых n строк файла
  - `Get-Content -Tail n` для чтения последних n строк
- команды 'grep' не использовать. использовать findstr или Select-String

**Пример:**
```powershell
cd path; python script.py    # ✅ Правильно
cd path && python script.py  # ❌ Ошибка в PowerShell
```

---

## 📁 Работа с файлами

### Создание файлов
- ❌ **НЕ создавать** тестовые скрипты/данные без явного запроса
- ❌ **НЕ создавать** `*.md` файлы
- ❌ **НЕ дублировать** существующие файлы проекта
- ✅ При создании файлов учитывать структуру проекта в IDEA

---

## 🧪 Тестирование

### Smoke-тесты (`utest.py`)
**Расположение:** `src/main/python/utest.py`

**Запуск:**
```powershell
cd c:\dev\github.com\vadim-kosarev\rgzz\src\main\python
python utest.py
```

### Когда добавлять тесты
- ✅ При изменении ключевой функциональности (DTO, алгоритмы, API)
- ✅ Для каждого нового скрипта — проверка запускаемости из консоли
- ✅ После серьёзных изменений кода — обязательно запускать `utest.py`

---

## 🗄️ База данных

### PostgreSQL (Frigate)
**Подключение:**
- Host: `brightsky:5433`
- User: `brightsky`
- Password: `brightsky`

**Использование:** только для получения справочной информации по API frigate и примеров данных

**DDL структура:** `database/postgres-init/*.sql`

---

## 🎸 Мета-команды

* использовать файлы для хранения информации о коммите и результатах выполненных команд
  * commit_info.txt
  * commit_git_status.txt
  * commit_git_work.txt
  * tmp.tmp - для сохранения истории изменений

### Команда: `балалай`
**Действие:** Сгенерировать читаемый комментарий к коммиту в git и закоммитить
* запустить скрипт для подготовки информации о коммите:
  ```powershell
  python src/main/python/prepare_commit.py
  ```
* использовать минимум команд, требующих подтверждений для запуска
* комментарий к коммиту сжать по смыслу до примерно 5 строчек максимум

### Команда: `пуш`
**Действие:** запушить текущую ветку в удалённый репозиторий
* использовать минимум команд, требующих подтверждений для запуска

---

## 📋 Checklist при работе

- [ ] Код на английском, комментарии на английском
- [ ] Общение с пользователем на русском
- [ ] Использую `;` вместо `&&` в PowerShell
- [ ] После изменений запустил `utest.py`
- [ ] Не создавал лишние тестовые файлы
- [ ] Учел структуру проекта при создании файлов
